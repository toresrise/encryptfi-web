import React from 'react'
import { TitleSection } from '~/components/global/TitleSection'

export const Upload: React.FC = () => (
    <section>
        <TitleSection title={'upload.title'} />
    </section>
)
